<Window x:Class="Revalis.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Revalis"
        mc:Ignorable="d"
        Title="Revalis" Height="450" Width="800" WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        AllowsTransparency="False"
        Background="Black">
    <Grid MouseLeftButtonDown="Header_MouseLeftButtonDown">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/> <!-- Title bar -->
            <RowDefinition Height="25"/> <!-- Menu bar -->
            <RowDefinition Height="*"/> <!-- Main Content -->
        </Grid.RowDefinitions>
        
        <!-- Title Bar -->
        <DockPanel Grid.Row="0" Background="#2d2d2d" LastChildFill="False">
            <TextBlock Text="Revalis"
                       VerticalAlignment="Center"
                       Foreground="White"
                       FontWeight="Bold"
                       Margin="10,0"
                       DockPanel.Dock="Left"/>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="_" Width="40" Command="{Binding MinimizeCommand}"/>
                <Button Content="□" Width="40" Command="{Binding MaximizeCommand}"/>
                <Button Content="X" Width="40" Command="{Binding CloseCommand}"/>
            </StackPanel>
        </DockPanel>

        <Menu Grid.Row="1">
            <MenuItem Header="_File">
                <MenuItem Header="Save Database" Command="{Binding SaveDbCommand}"/>
                <MenuItem Header="Import Database" Command="{Binding ImportDbCommand}"/>
                <Separator/>
                <MenuItem Header="Exit" Command="{Binding CloseCommand}"/>
            </MenuItem>
            <MenuItem Header="_Library">
                <MenuItem Header="My Games" Command="{Binding OpenUserLibraryCommand}"/>
            </MenuItem>
            <MenuItem Header="_Search">
                <MenuItem Header="Search Revalis API" Command="{Binding SearchRevalisCommand}"/>
            </MenuItem>
            <MenuItem Header="_Future">
                <MenuItem Header="Link Xbox Live" Command="{Binding LinkXboxCommand}"/>
                <MenuItem Header="Link PlayStation Plus" Command="{Binding LinkPlayStationCommand}"/>
                <MenuItem Header="Link Steam" Command="{Binding LinkSteamCommand}"/>
            </MenuItem>
        </Menu>

        <!-- Main Contnent -->
        <ContentControl x:Name="MainControl" Grid.Row="2" Content="{Binding CurrentView}"/>
    </Grid>
</Window>
