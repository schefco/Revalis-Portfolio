<UserControl x:Class="Revalis.Views.UserLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Revalis.Views"
             Background="#1e1e1e">

    <UserControl.Resources>
        <!-- Hop and hold style -->
        <Style TargetType="Border" x:Key="GameCardStyle">
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <!-- Mouse enters: scale up -->
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                             To="1.05" Duration="0:0:0.15" FillBehavior="HoldEnd"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                             To="1.05" Duration="0:0:0.15" FillBehavior="HoldEnd"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <!-- Mouse leaves: scale back down -->
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                             To="1" Duration="0:0:0.15"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                             To="1" Duration="0:0:0.15"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <ScrollViewer>
            <ItemsControl ItemsSource="{Binding MyGames}">
                <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <!-- Card style layout for library view -->
                    <WrapPanel IsItemsHost="True" Margin="10" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource GameCardStyle}"
                            BorderBrush="Gray"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="10"
                            Margin="8"
                            Background="#2a2a2a"
                            Width="200"
                                MouseLeftButtonUp="Card_Click">
                            <StackPanel>
                                <Image Source="{Binding CoverImageUrl}" 
                                       Height="180" 
                                       Stretch="UniformToFill"/>
                            <TextBlock Text="{Binding Name}" 
                                       FontWeight="Bold" 
                                       Foreground="White" 
                                       FontSize="14"/>

                            <!-- Remove button -->
                                <Button Content="Remove"
                                    Command="{Binding DataContext.RemoveGameCommand, 
                                              RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Margin="0,8,0,0"
                                    Background="#444"
                                    Foreground="White"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
</UserControl>
